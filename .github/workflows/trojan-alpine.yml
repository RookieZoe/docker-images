name: trojan-alpine
on:
  schedule:
    - cron: '0 0 * * *'

  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Set env
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          echo "Hello"
